# PROJECT_STATE.md — 项目当前状态

> 回答：一个完全不知道历史聊天的新会话，现在进入仓库最少要知道什么？
> 只保留当前状态，不写历史流水。Last Updated: 2026-09-23

## Current Phase

**维护期（Phase 1/2 已全部完成并经用户验收，Phase 3 暂缓）。**
用户 2026-09-18 决定：功能先开发到这一步，暂不推进 Phase 3。
2026-09-20 完成课表与概览 Single Viewport 一页式改造；2026-09-23 完成打卡页交互增强、
进度轴、课表手势，并把调课覆盖从本地缓迁移到云库。

## Current Objective

保持现状可用。无排期中的开发目标；后续仅做必要维护（bug 修复、兼容性保障）。

## Current Task

无进行中 Task。无下一个 Task。

## 当前实现状态

- 三个 tab 页全部可用且经用户验收：打卡（index）、课表（schedule）、概览（overview）+ custom-tab-bar。
- **打卡页**：确认弹窗（提交/撤销共用一套自绘弹窗）、撤销按钮（常驻，无可撤销时置灰）、
  横向进度轴（只读的班级实际进度指示器，推荐课件居中）、一屏免滚动布局。
- **课表页**：左右滑动切周（左滑=后一天，右滑=前一天，边界回弹）、点课程卡片调课、一屏免滚动。
- 业务数据：
  - 云集合 `progress`：打卡记录（客户端直写，无业务云函数）。
  - 云集合 `schedule`：调课覆盖，**两人共享、当周自动失效**（见 D015）。
- 业务配置唯一来源：`miniprogram/config/constants.js`（pptList 23 项含 totalPage/shortLabel/shortName、
  initialSchedule、timeSlots、formatDate、mondayTag）。
- 本地缓存：**只剩** `legacyPagesFixed_v2`（回补一次性标记）。调课已不在本地。
- 云库权限：**`progress` 与 `schedule` 均为「所有用户可读，仅创建者可读写」**。
  预设下「创建」永远允许，所以任何微信用户都能新增调课文档 —— **决定改为 openid 白名单，见 D009（未执行）
  且必须同时应用到两个集合**。
- 打卡智能推荐：三级分流匹配（上课中 -> 刚下课60分钟内 -> 离当前最近未来第一节课），
  与云端调课覆盖联动（见 D010、D015）。
- 进度轴语义：**按真实记录点亮**，不用位置切片 —— 跳着上课时被跳过的课件保持灰（见 D013）。
- 页面单屏一览：三个页面均锁定 100vh，纵向不滚动（见 D011、D014）。
- 日期统一标准化：全项目日期写入与展示统一 `YYYY-MM-DD`（`formatDate`）。
- 包体 85K（模板 UI 残留已清，详见 `docs/ARCHITECTURE.md`）。AppID `wx7d0cddb68b034b42`，用户确认自有。
- 云函数 `cloudfunctions/quickstartFunctions` = CloudBase 模板 sales 演示代码，**业务零依赖**
  （未删，不在小程序包内、对包体零影响）。
- Git：main 分支，与 https://github.com/Tjl925/ClassManager 同步。

## 已验证结果（全部，截至 2026-09-23）

- 全部 JS 通过 `node --check`；23 条 totalPage / shortLabel / shortName 映射逐条核对一致。
- **打卡页离线状态机测试 45 项全通过**（桩掉 Page/wx/云库直接 require 页面代码）：确认弹窗拦截写库、
  连点只写一条、撤销删对目标且只删一次、跳着上课不误点亮、`partial` 不算讲过、分页取全、
  `onShow` 重置进度轴、首次进入不与 smartRecommend 抢。
- **调课云端存储测试 19 项全通过**：`mondayTag` 周一边界（含跨年）、同周 tag 一致、
  跨周自动失效、同键取最新、取消课标记、55 条分页取全、写入带本周 tag。
- **课表手势测试 16 项全通过**：方向、边界回弹、竖滑/斜滑拒绝、tap 冲突吞掉、
  点 pill 与滑动共用同一路径。
- 进度轴居中算法独立复算：5 种屏宽 × 23 节点，偏差 0.0000px，无越界。
- 换行容量复算：23 个课件名最多 3 行，末行余量 320%。
- 垂直预算复算：5 种机型全部一屏容纳（最紧的 iPhone SE「未讲完」态余 56rpx）。
- 配色三件套（#502BD8/#00C48C/#FF7B52）过 dataviz 校验。
- 模板残留清理：删除前验证 40 个文件全部零引用；清理后用户开发者工具复验通过（2026-09-18）。
- 用户开发者工具验收通过（2026-09-23）：确认弹窗、撤销、进度轴跳课点亮、
  课表手势、一屏布局、调课云端共享。

## 当前已知问题（低优先级，未排期）

1. 云函数模板残留（sales 演示代码）。无害，但不要误当业务代码。
2. `overview.js` 查询失败仅 console.error，回退占位卡不提示（有意为之）。
3. **上线前必须收敛写权限**：`schedule` 集合在预设权限下任何微信用户都能新增调课文档。
   对策见 D009，**规则要同时应用到 `progress` 和 `schedule`**。
4. 跨账号删除受限于「仅创建者可读写」：本机只能删自己账号创建的记录。D009 落地后消除。

## 当前阻塞项

无。

## 当前尚未验证的假设

1. **D009 安全规则语法**：`auth.openid in ['openidA','openidB']` 的确切写法未在控制台编辑器实测确认。
   不照抄文档，以编辑器提示为准。
2. **两人的 openid**：均尚未取得。D009 的执行顺序不可颠倒（先取 openid，再改规则）。

## 当前活跃约束

KISS / 无 npm / 不新增业务云函数 / 不改 progress 字段名 / UI 设计系统不可破坏 /
**小程序端单次查询最多 20 条，分页步长必须等于 20**（详见 `AGENTS.md` §3–§5）。

## Next Steps

**待执行事项**：

1. **上线路径**：开发者工具「上传」→ 公众平台设为体验版 → 加女友为体验成员 → 她扫码。
   公开发布需填服务类目并提审；个人主体可选类目范围**未验证**，须在公众平台实际确认。
2. **落地 D009 openid 白名单**（纯控制台配置，零代码改动）。顺序不可颠倒：
   ① 控制台取老师 `_openid`（从 `progress` 已有记录）→ ② 让女友打一条卡取她的 `_openid`
   → ③ 再改规则 → ④ 验证。**规则必须同时应用到 `progress` 和 `schedule`**。
3. **注意**：openid **不得写入本仓库**（仓库已推送到公开 GitHub），只存在云端安全规则里。

若用户提出新需求：先确认方向 → `PLAN.md` 细化为 Task → 开工；开工前重读 `AGENTS.md` §4–§5 约束。

## Verification Status

| 项 | 状态 |
|---|---|
| JS 语法（全部业务文件） | ✅ 通过 |
| totalPage / shortLabel / shortName 23 条映射 | ✅ 一致 |
| 打卡页状态机测试（45 项） | ✅ 全部通过（2026-09-23） |
| 调课云端存储测试（19 项） | ✅ 全部通过（2026-09-23） |
| 课表手势测试（16 项） | ✅ 全部通过（2026-09-23） |
| 进度轴居中算法（5 屏宽 × 23 节点） | ✅ 偏差 0.0000px |
| 垂直预算（5 机型一屏） | ✅ 全部容纳 |
| 概览页排序/差距逻辑（模拟数据） | ✅ 正确 |
| 配色校验（dataviz 脚本） | ✅ 通过 |
| 用户开发者工具验收 | ✅ 通过（2026-09-23） |
| 待验证项 | D009 规则语法（未执行）；两人 openid 未取得 |
