# PROJECT_STATE.md — 项目当前状态

> 回答：一个完全不知道历史聊天的新会话，现在进入仓库最少要知道什么？
> 只保留当前状态，不写历史流水。Last Updated: 2026-09-20

## Current Phase

**维护期（Phase 1/2 已全部完成并经用户验收，Phase 3 暂缓）。**
用户 2026-09-18 决定：功能先开发到这一步，暂不推进 Phase 3。
2026-09-20 完成课表与概览 Single Viewport 一页式改造，彻底消除纵向滚动；修复打卡智能推荐跨天/周末算法。

## Current Objective

保持现状可用。无排期中的开发目标；后续仅做必要维护（bug 修复、兼容性保障）。

## Current Task

无进行中 Task。无下一个 Task。

## 当前实现状态

- 三个 tab 页全部可用且经用户验收：打卡（index）、课表（schedule，一页式）、概览（overview，一页式排名进度对比）+ custom-tab-bar。
- 业务数据：云库 `progress` 集合（客户端直写，无业务云函数）。
- 业务配置唯一来源：`miniprogram/config/constants.js`（pptList 23 项含 totalPage、initialSchedule 四班课表、timeSlots 11 节次）。
- 本地缓存：`customSchedule`（调课覆盖）、`legacyPagesFixed`（回补标记）。
- 云库 `progress` 权限：**「所有用户可读，仅创建者可读写」**（2026-09-18 用户念的原文）。读不过滤用户，第二人写入会混入四班视图。**决定改为 openid 白名单，见 D009（未执行）。**
- 打卡智能推荐：三级分流匹配（上课中 -> 刚下课60分钟内 -> 离当前最近未来第一节课），支持 `customSchedule` 调课联动（见 D010）。
- 页面单屏一览（Single Viewport）：课表页与概览页锁定 100vh，flex 自适应均分，移除化学课三字与副标题，禁止纵向滚动（见 D011）。
- 日期统一标准化：全项目打卡记录写入与展示统一使用 `YYYY-MM-DD` 格式，彻底消除英文字符串与斜杠混用。
- 包体 85K（模板 UI 残留已清，详见 `docs/ARCHITECTURE.md`）。AppID `wx7d0cddb68b034b42`，用户确认自有。
- 云函数 `cloudfunctions/quickstartFunctions` = CloudBase 模板 sales 演示代码，**业务零依赖**（未删，不在小程序包内、对包体零影响）。
- Git：main 分支，与 https://github.com/Tjl925/ClassManager 同步。

## 已验证结果（全部，截至 2026-09-20）

- 全部 JS 通过 `node --check`；23 条 totalPage 映射逐条核对一致。
- 智能推荐算法 9 组边界用例（周末夜间/早晨/课前/课中/刚下课/午间/调课改班/调课取消）自动化测试 100% 通过。
- 日期格式化兼容性测试 7 组用例（英文/斜杠/横杠/Date/时间戳/云对象）全部通过。
- 概览页排序/差距/百分比逻辑经模拟数据验证 + 用户目检。
- 配色三件套（#502BD8/#00C48C/#FF7B52）过 dataviz 校验。
- 模板残留清理：删除前验证 40 个文件全部零引用；清理后用户开发者工具复验通过（2026-09-18）。
- 课表页与概览页 Single Viewport 一页式结构验证无溢出、无绝对定位坍塌。

## 当前已知问题（低优先级，未排期）

1. 云函数模板残留（sales 演示代码）。无害，但不要误当业务代码。
2. `overview.js` 查询失败仅 console.error，回退占位卡不提示（有意为之）。
3. **第二人使用风险（已有对策，未落地）**：云库权限为"所有用户可读"，第二人打卡的记录会混入四班进度视图，且因"仅创建者可写"老师删不掉。**对策已定为 D009 的 openid 白名单，待执行**；执行后两人可互相改删记录，此风险消除。

## 当前阻塞项

无。

## 当前尚未验证的假设

1. **D009 安全规则语法**：`auth.openid in ['openidA','openidB']` 的确切写法未在控制台编辑器实测确认。不照抄文档，以编辑器提示为准。
2. **女友的 openid 归属**：尚未从控制台读到，两人 openid 均未取得。

## 当前活跃约束

KISS / 无 npm / 单用户 / 不新增业务云函数 / 不改 progress 字段名 / UI 设计系统不可破坏（详见 `AGENTS.md` §4–§5）。

## Next Steps

**当前唯一待执行事项：落地 D009 openid 白名单**（纯控制台配置，零代码改动）。顺序不可颠倒：

1. 控制台 → 数据库 → `progress` → 点开任意一条已有记录，抄老师本人 `_openid`。
2. 让女友在**当前宽松权限下**打一条卡 → 控制台取她那条记录的 `_openid`。
3. 数据库 → 权限设置 → 自定义安全规则：`read: true`，`write` 填两个 openid（**语法以编辑器实际提示为准，不照抄文档**）。
4. 验证：女友仍能打卡；未列入白名单的账号写不进去。
5. 执行后回报实际结果 → 更新本文档与 D009 状态。

**上线路径（与上述独立）**：开发者工具「上传」→ 公众平台设为体验版 → 加女友为体验成员 → 她扫码。
公开发布需填服务类目并提审；个人主体可选类目范围**未验证**，须在公众平台实际确认。

**注意**：openid **不得写入本仓库**（仓库已推送到公开 GitHub），只存在云端安全规则里。

若用户提出新需求：先确认方向 → `PLAN.md` 细化为 Task → 开工；开工前重读 `AGENTS.md` §4–§5 约束（KISS / 无 npm / 不改 `progress` 字段名 / UI 设计系统）。

## Verification Status

| 项 | 状态 |
|---|---|
| JS 语法（全部业务文件） | ✅ 通过 |
| totalPage 23 条映射 | ✅ 一致 |
| 推荐算法 9 项矩阵测试（含周日 22:16 与调课） | ✅ 全部通过（2026-09-20） |
| 课表与概览 Single Viewport 免滑动重构 | ✅ 代码与样式验证通过（2026-09-20） |
| 概览页排序/差距逻辑（模拟数据） | ✅ 正确 |
| 配色校验（dataviz 脚本） | ✅ 通过 |
| 模板残留清理后编译/三页/tab 栏 | ✅ 用户开发者工具复验通过（2026-09-18） |
| 待验证项 | 无 |